# =============================================================================
# Python
# =============================================================================
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
downloads/
eggs/
.eggs/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# 虚拟环境
.venv/
venv/
ENV/
env/
.env

# =============================================================================
# 本项目的构建产物 —— 绝对不要提交
# =============================================================================

# 绿色便携版：约 300 MB，里面全是 Python 二进制 dll。
# 提交了会永久留在 git 历史里，仓库再也瘦不下来。
# 需要的人自己跑 `python -m tools.build_portable` 生成。
dist/

# 打包缓存（嵌入式 Python 压缩包等）
build/cache/

# Excel 模板是生成物，不是源码。跑 `python -m tools.build_excel` 即可产出。
output/

# 样例数据是生成物。跑 `python -m core.sample_data` 即可产出。
data/sample/

# =============================================================================
# ⚠️ 真实业务数据 —— 最重要的一条
# =============================================================================
# 客户的投放数据、折扣、CPRP 都是商业机密，提交到公开仓库等于泄密。
# 下面的规则默认屏蔽所有数据文件，只对样例数据的目录结构放行。

data/**
!data/.gitkeep
!data/README.md

# 兜底：仓库任何位置的数据文件一律不收
*.xlsx
*.xls
*.xlsm
*.csv
*.parquet
*.db
*.sqlite3

# 如果确实需要提交某个文件，用 git add -f 显式强制添加（会让你多想一秒）

# =============================================================================
# 敏感配置
# =============================================================================
# 校准后的真实基准（含公司真实 CPRP / 报价）不要提交
config/benchmarks.local.yaml
config/*.local.yaml
*.secret
*.key
.env.local

# =============================================================================
# 编辑器 / 系统
# =============================================================================
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
desktop.ini

# =============================================================================
# Streamlit
# =============================================================================
.streamlit/secrets.toml

# =============================================================================
# 临时文件
# =============================================================================
*.log
*.tmp
~$*
